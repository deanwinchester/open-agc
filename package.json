{
  "name": "open-agc",
  "private": true,
  "scripts": {
    "build": "vite build --config vite.config.mjs",
    "watch": "vite build --watch --config vite.config.mjs"
  },
  "devDependencies": {
    "terser": "^5.47.1",
    "vite": "^6.3.5"
  }
}
