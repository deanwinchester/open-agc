{
  "name": "python_run_code",
  "description": "Agent can execute Python code via REPL",
  "prompt": "用 Python 计算 1到100的和，告诉我结果",
  "expected": {
    "tool_used": ["execute_python"],
    "output_contains": ["5050"],
    "max_steps": 5,
    "max_tokens": 8000
  },
  "timeout": 30,
  "tags": ["core", "python"]
}
